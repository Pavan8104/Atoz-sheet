Deques(Std:: deques)
Doubly ended queus
Common Functions: It supports push_front(), pop_front(), push_back(), and pop_back(), along with other standard functions like size(), empty(), and at().


#include<iostream>
#include<dequeue>
using namespace std;

int main(){
dequeue<int> dq;
dq.push_back(1);
dq.emplace_back(2);
dq.emplace_front(4);
dq.pop_back();
dq.pop_front():
cout<<"Back element :"<<dq.back() << endl;
cout<<"Front element :" << dq.front() << endl;
return 0;
}
